#ifndef ENCODE_H
#define ENCODE_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Erstelle eine Funktion, die einen String encoded
const char *encode(const char *string);

#endif // Encode_h